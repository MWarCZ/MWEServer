
type Query {
  # UserAdmin => Vsechny mimo skryte (removed)
  # SuperUserAdmin => Zcela vsechny
  # User => Jen sebe
  # Other => Nic
  user(filter: UserFilterInput!): User
  # UserAdmin => Vsechny mimo skryte (removed)
  # SuperUserAdmin => Zcela vsechny
  # User => Jen sebe
  # Other => Nic
  users: [User]!
}
type Mutation {
  # User
  changeUserPassword(input: ChangePasswordInput!, filter: UserFilterInput!): Boolean
  updateUserInfo(input: UpdateUserInfoInput!, filter: UserFilterInput!): User
  # UserAdmin
  ## updateUserInfo => Pro vsechny uzivatele
  createNewUser(input: NewUserInput!): User # vytvorit uzivatele
  removeUser(filter: UserFilterInput!): Boolean # skrit uzivatele s id
  lockUser(filter: UserFilterInput!): User
  unlockUser(filter: UserFilterInput!): User
  resetUserPassword(filter: UserFilterInput!): String
  # SuperUserAdmin
  deleteUser(filter: UserFilterInput!): Boolean # trvale odstranit
  recoverUser(filter: UserFilterInput!): User # obnovit skriteho uzivatele s id


}

type User {
  id: Int!
  login: String!
  email: String!
  firstName: String!
  lastName: String!
  membership: [Member!]!
  protected: Boolean!
  locked: Boolean!
  removed: Boolean!
}

input UserFilterInput {
  id: Int
  login: String
}

input NewUserInput {
  password: String!
  login: String!

  email: String
  firstName: String
  lastName: String
}

input UpdateUserInfoInput {
  email: String
  firstName: String
  lastName: String
}

input ChangePasswordInput {
  oldPassword: String!
  newPassword: String!
}
