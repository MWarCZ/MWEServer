
# type Query {

# }
type Mutation {
  addMember(groupId: Int!, userId: Int!): Member
  removeMember(groupId: Int!, userId: Int!): Member
  setMemberPermisions(input: MemberPermisionsInput): Member # Nastaveni lokalnich prav uzivatele

}

type Member {
  id: Int!
  group: Group!
  user: User!
  protected: Boolean!
  addMember: Boolean!
  removeMember: Boolean!
  showMembers: Boolean!
}
input MemberPermisionsInput {
  addMember: Boolean
  removeMember: Boolean
  showMembers: Boolean
}
